# Copy to ".env" and fill in secrets. Do not commit ".env".

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Used for the local vector index (HTTP API — no torch/faiss DLLs).
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

HUGGINGFACE_API_KEY=
