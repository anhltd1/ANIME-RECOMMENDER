"""Streamlit frontend package.

Place the main Streamlit entry script (e.g. ``streamlit run app/main.py``) and any UI-only
helpers here. Call into ``src`` for backend and AI logic; keep presentation separate from
domain code.
"""
