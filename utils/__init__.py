"""Project-wide utilities (logging, exceptions, shared helpers).

Subpackages group code by concern; import from ``utils.<subpackage>`` in app, config, and
``src`` when those layers need shared infrastructure.
"""
